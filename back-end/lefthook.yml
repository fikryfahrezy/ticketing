pre-commit:
  parallel: true
  jobs:
    - root: "back-end/"
      stage_fixed: true
      glob:
        - "*.md"
        - "*.json"
        - "*.yaml"
      run: npx prettier --write {staged_files}

    - root: "back-end/"
      stage_fixed: true
      glob:
        - "*.html"
        - "*.css"
        - "*.tsx"
        - "*.jsx"
      group:
        piped: true
        jobs:
          - run: npx eslint {staged_files}
          - run: npx prettier --write {staged_files}
      